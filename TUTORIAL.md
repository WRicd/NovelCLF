# 从零开始理解 NovelCLF

这份教程面向第一次完整实践机器学习项目的学习者。我们把目标定得很具体：输入一段中文小说文本，预测它更像都市、科幻、历史还是玄幻。

## 1. 为什么不能直接用 12 本小说训练

当前项目每个类别有 3 本小说，一共 12 本。如果把一本小说当作一个样本，那么总样本数只有 12 条。

机器学习模型需要从样本中总结规律。12 条样本太少，训练集和测试集怎么划分都会非常不稳定。一次测试分数高，不一定说明模型真的学会了类型分类；它可能只是碰巧猜对了几本书。

## 2. 为什么要做文本分块

小说是长文本，一本书可能有几百万字。我们可以把每本书切成 1000 字左右的段落，每个段落继承原书的类别标签。

这样做以后，项目从 12 个整本小说样本，变成几千个段落样本。模型训练会更稳定，也更适合 TF-IDF、SVM、BERT 这类文本分类方法。

## 3. 分块以后还有什么风险：数据泄漏（已证实）

最大的风险是数据泄漏。如果同一本书的段落同时出现在训练集和测试集里，模型可能记住了书名、人物名、地名，而不是真正学会了“类型”。

所以本项目提供两种评估：
- `chunk_random`：随机切分段落，适合快速验证流程。
- `book_holdout`：按小说分组切分，测试集中的整本书不出现在训练集里，结果更可信。

> [!WARNING]
> **真实实验验证了这一点**：在我们第一阶段的实验中，当使用 `chunk_random` 时，朴素贝叶斯和 SVM 模型几乎都能达到 **99% - 100%** 的准确率。但当我们使用 `book_holdout` 时，准确率断崖式下跌到 **14% - 40%**。
>
> 我们通过提取特征重要性发现，都市类决定性特征是`林逸`、`苏玥`，玄幻类是`张若尘`、`龙皓晨`。模型根本没有学习什么是“科幻”或“都市”，它仅仅是**背下了每本书的主角名字**！

## 4. 什么是 TF-IDF（字符 vs 分词）

TF-IDF 是一种把文本转换成数字特征的方法。它关注一个词或字在当前文本中出现得多不多，以及在所有文本中是不是太常见。

本项目支持两种 TF-IDF：
- **字符 n-gram (`nb`, `svm_tfidf`)**：以连续的字为特征，不受分词器词库影响，在网络小说这种充满自造词（人名、功法）的场景下，往往泛化能力更稳。
- **jieba 分词 (`nb_word`, `svm_tfidf_word`)**：以真实的中文词组为特征，可解释性强（你能直接看出模型依赖了哪个词），但受限于词典准确度。

## 5. 跨越局限：第二阶段的深度 ML 诊断

为了解决简单模型无法真正理解“语义”、容易在单段落上猜错的问题，我们在项目中实现了更多工业级方法：

- **整本书多数投票（Majority Voting）**：单凭一个段落很难判断类型，所以我们对一本书的所有段落分别预测，最后统计得票最多的类别。即便部分段落识别错误，“群智”也能纠正偏差。（可通过 CLI 的 `--voting` 参数体验）
- **Leave-One-Book-Out 交叉验证**：`book_holdout` 每次只抽几本书，结果具有偶然性。我们实现了 LOBO 交叉验证，轮流让每一本书做测试集，求平均准确率，这是最严谨的泛化评估。
- **学习曲线（Learning Curve）**：绘制不同训练样本量下的训练集和测试集准确率，用于直观诊断模型是欠拟合（需要更复杂的模型，如 BERT）还是过拟合（需要更多数据或正则化）。

## 6. 从统计机器学习到深度学习

TF-IDF 始终是基于字词统计的。想要真正捕捉“修仙氛围”或“赛博朋克感”，我们需要预训练语言模型。

本项目内嵌了 `BERT + SVM` 的流水线：先用 BERT 将文字降维成充满语义信息的密集向量（Embedding），并将这 4800 个段落的特征持久化缓存，再用 SVM 做最终分类。这是非常经典且高效的微调策略，既保留了深度模型的强大，又省去了全量微调所需的昂贵算力。

## 7. 下一步实践建议

现在你拥有了一个完整的实验台，你可以：
- 上传你喜欢的小说 txt 文件，在 Streamlit 中测试各种模型对它的分类。
- 运行 `scripts/plot_learning_curve.py` 观察过拟合现象。
- 为你的数据集增加更多风格差异更小的书（如“传统武侠” vs “高武玄幻”），挑战模型的极限！
